[package]
name = "mlx-gen-sana"
version = "0.0.0"
edition = "2021"
description = "SANA (NVlabs) provider crate for mlx-gen. Spike sc-8486: DC-AE f32 deep-compression decoder."
license = "Apache-2.0"
repository = "https://github.com/michaeltrefry/mlx-gen"
keywords = ["mlx", "apple-silicon", "image", "sana", "inference"]
categories = ["science"]
autotests = false

[dependencies]
mlx-gen = { path = ".." }
# Must match the core crate's MLX binding exactly (shared `Array` type). See root Cargo.toml.
mlx-rs = { workspace = true }
# SANA's text conditioning REUSES PiD's already-native gemma-2-2b-it CHI caption encoder (epic 8485,
# sc-8488): the two architectures share the exact Gemma-2 last-hidden CHI text-encoder lineage, so
# we depend on `mlx-gen-pid` for `Gemma2`/`Gemma2Config`/`CaptionEncoder` rather than duplicating it.
mlx-gen-pid = { path = "../mlx-gen-pid" }

[dev-dependencies]
# The shared weights-free conformance suite (epic SC-22657): the crate's contract tests assert
# the provider-declared architecture axes against it.
sceneworks-gen-core-testkit = { workspace = true }
# RAII fixture roots for the test helpers (sc-17791). A `TempDir` guard removes the tree on
# `Drop` — including out of a panicking test, which is the only path the old
# `env::temp_dir().join(..)` + trailing `remove_dir_all` shape ever leaked on.
tempfile = { workspace = true }
# Persist fit comparisons and real-route preview strips in ignored acceptance tests.
image = { workspace = true }
# Bind each `MEMORY_EVIDENCE_V1` record to the exact pixels it was measured over (sc-15523).
sha2 = { workspace = true }

# sc-21383: one integration-test binary per crate. `tests/main.rs` is generated by
# scripts/ci/consolidate_integration_tests.py; add a `tests/<name>.rs` file and run
# `python3 scripts/ci/consolidate_integration_tests.py regen`. `cargo test --test integration
# -- <name>::` selects one former per-file target.
[[test]]
name = "integration"
path = "tests/main.rs"
