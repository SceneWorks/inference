# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-27 19:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust repository containing a crate (candle-gen-boogu) that implements a generative image model backend, likely a diffusion model variant (possibly Flux or similar architecture). It integrates with a gen_core framework and leverages the Candle machine learning framework (Hugging Face's Rust ML framework) for tensor operations and model inference. The crate provides: model loading and configuration, pipeline scheduling (noise/sigma schedules used in diffusion processes), image editing capabilities, and a Generator trait implementation supporting both text-to-image and image-editing generation modes. As a single-project repository, there are no internal cross-project dependencies. Externally, it depends on the Candle ML framework for low-level tensor/neural network operations and a gen_core crate that defines the Generator trait and shared abstractions. It is likely consumed by higher-level applications or services that need GPU-accelerated generative image inference in Rust.

## Projects

### candle-gen

candle-gen-boogu is a Rust crate implementing a specific generative image model backend (likely a diffusion model variant, possibly Flux or similar) that integrates with a gen_core framework. It provides model loading, configuration, pipeline scheduling (noise/sigma schedules), and image editing capabilities, exposing a Generator trait implementation that handles both text-to-image and image-editing generation modes.

