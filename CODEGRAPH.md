# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-05 23:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based image generation inference library built on top of the Candle ML framework. It implements a diffusion model pipeline architecture supporting image generation and editing workflows. The repository provides abstractions for model configuration, weight loading, noise scheduling (sigma/timestep management), and a generator interface that handles reference-image conditioning for edit operations. A 'boogu' submodule implements a specific model variant with its own configuration, loader, pipeline, and generator registration, suggesting the codebase is designed to support multiple model backends under a common interface. The project appears to be a backend inference engine rather than a user-facing application, likely intended to be consumed as a library or service. It has no observed cross-repository dependencies beyond the Candle ML framework itself.

## Projects

### candle-gen

candle-gen is a generative image model inference project (likely a diffusion model backend) built on the Candle ML framework. It implements a pipeline for image generation and editing, including model configuration, weight loading, noise scheduling (sigmas/timesteps), and a generator interface that handles reference-image conditioning for edit operations. The 'boogu' submodule appears to be a specific model variant with its own config, loader, pipeline, and generator registration logic.

