# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-03 20:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/binary that implements an image generation backend using the Candle ML framework (Hugging Face's Rust-native tensor/inference library). It provides a Generator trait abstraction for loading and running diffusion model variants (identified as 'Boogu', with base, turbo, and edit model types), managing model component lifecycles, processing image generation requests with progress callback support, and validating configuration and reference image inputs for image editing workflows. The project appears to be a self-contained inference engine for a specific diffusion model, wrapping Candle's lower-level ML primitives into a higher-level generation API. There is one primary project in the repository, so all logic is co-located. No cross-repository dependencies were identified, suggesting it either operates standalone or is consumed as a library by an upstream orchestration layer not visible in this analysis. It depends on the Candle ML ecosystem for tensor operations and model execution.

## Projects

### candle-gen

candle-gen is a Rust firmware/inference project that implements a image generation backend (likely for a diffusion model called 'Boogu') using the Candle ML framework. It provides a Generator trait implementation capable of loading model variants (base, turbo, edit), managing model components, handling generation requests with progress callbacks, and validating configuration and reference image inputs for image editing workflows.

