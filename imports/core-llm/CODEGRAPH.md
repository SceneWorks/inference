# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 00:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library repository that defines a trait-based abstraction layer for interacting with text-based large language models (LLMs). The repository consists of two related crates: (1) core-llm, which provides the core traits and types (such as TextLlm) that model a uniform interface for LLM interactions including streaming, cancellation, seed-based determinism, multimodal input, and input validation; and (2) core-llm-testkit, a conformance testing library that provides a reusable suite of behavioral test cases any TextLlm implementation must satisfy. The testkit includes stub/mock implementations for self-testing and verifies behaviors like streaming output, request cancellation, seed determinism, multimodal handling, and validation error handling. The two crates work together so that downstream implementors of the TextLlm trait can use core-llm-testkit to verify their implementations conform to the expected contract. No external repository dependencies or dependents were identified; the repository appears to be a foundational abstraction library intended to be consumed by other projects that integrate specific LLM backends.

## Projects

### core-llm

core-llm is a Rust library project providing a trait-based abstraction for text-based large language model (LLM) interactions, along with a testkit (core-llm-testkit) for conformance testing of TextLlm implementations. The testkit defines a suite of behavioral checks (streaming, cancellation, seed determinism, multimodal, validation, etc.) that any TextLlm implementation must satisfy, plus stub implementations and self-tests to verify the testkit itself works correctly.

