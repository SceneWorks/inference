# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 18:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library that defines abstractions and interfaces for interacting with large language models (LLMs). It provides a common API surface for LLM providers, covering capabilities such as streaming text generation, multimodal inputs, tool/function calling, seed-based determinism, thinking/reasoning modes, and input validation. The repository contains two crates: the main `core-llm` crate (which defines the core traits, types, and interfaces) and `core-llm-testkit` (a companion testing crate that provides a stub/mock LLM implementation and a suite of conformance check functions). The testkit allows downstream LLM provider implementations to verify that they correctly satisfy the contracts defined by the core library—covering areas like streaming behavior, cancellation handling, tool call correctness, registry and descriptor behavior, and more. There are no observed cross-repository dependencies; this library is designed to be depended upon by concrete LLM provider implementations that integrate it and use the testkit to validate conformance.

## Projects

### core-llm

core-llm is a Rust library providing abstractions and interfaces for interacting with large language models (LLMs), specifically text-based LLMs. The core-llm-testkit sub-crate provides a conformance testing framework with a stub implementation and a suite of check functions that verify LLM provider implementations correctly handle streaming, cancellation, multimodal inputs, tool calls, seed-based determinism, thinking modes, validation, and registry/descriptor behavior.

