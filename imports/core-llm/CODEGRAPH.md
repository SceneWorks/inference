# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 19:17 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library that provides trait-based abstractions for Large Language Model (LLM) text generation backends. It defines a common interface that different LLM provider implementations must conform to, along with a companion crate (core-llm-testkit) that supplies a standardized conformance test suite and a stub/mock implementation. The testkit validates behavioral correctness across capabilities including streaming responses, cancellation, multimodality (e.g., image inputs), seed-based determinism, input validation, and thinking/reasoning modes. The two crates work together as an abstraction layer and quality-assurance framework: core-llm defines the contracts, and core-llm-testkit allows downstream LLM backend implementors to verify their implementations satisfy those contracts. No external repository dependencies were detected, but the library is intended to be depended upon by concrete LLM provider crates that implement its traits and run its conformance tests.

## Projects

### core-llm

core-llm is a Rust library that defines abstractions and a conformance test suite for Large Language Model (LLM) text generation backends. The testkit subproject (core-llm-testkit) provides a standardized set of behavioral checks and a stub implementation that LLM provider implementations must satisfy, covering streaming, cancellation, multimodality, seed determinism, validation, and thinking/reasoning modes.

