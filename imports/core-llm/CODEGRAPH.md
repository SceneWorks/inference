# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 19:17 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library repository that defines a trait-based abstraction layer for Large Language Model (LLM) inference, centered on text generation. The repository contains two closely related crates: (1) the core library (`core-llm`) which defines the `TextLlm` trait and associated types as a stable interface that LLM backend implementations must satisfy, and (2) a testkit crate (`core-llm-testkit`) that provides conformance testing infrastructure for verifying that any `TextLlm` implementation correctly handles a defined set of behavioral contracts — including streaming output, cancellation, seed-based deterministic generation, multimodal inputs, thinking/reasoning modes, input validation, and backend registry/descriptor requirements. The two crates work together as a definition-and-verification pair: the core crate establishes the contract, and the testkit crate enforces it. There are no observed cross-repository dependencies, suggesting this repository is intended to be consumed as a dependency by other projects that implement LLM backends (e.g., wrappers around specific inference engines or cloud APIs), rather than depending on external domain-specific libraries itself.

## Projects

### core-llm

core-llm is a Rust library that defines a trait-based abstraction for Large Language Model (LLM) inference, specifically focused on text generation. The project includes a testkit (core-llm-testkit) that provides conformance testing infrastructure for implementations of the TextLlm trait, ensuring that any LLM backend correctly handles streaming, cancellation, seed-based determinism, multimodal inputs, thinking modes, validation, and registry/descriptor requirements.

