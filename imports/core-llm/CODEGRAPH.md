# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 01:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust repository that provides a trait-based abstraction layer for interacting with text-based Large Language Models (LLMs). It consists of two crates: (1) core-llm, which defines the core TextLlm trait and associated types that implementors must satisfy to provide a uniform interface over different LLM backends, and (2) core-llm-testkit, a conformance testing utility crate that allows implementors of the TextLlm trait to validate their implementations against a standardized suite of behavioral checks. The testkit covers areas such as streaming output, cancellation handling, deterministic seeding, multimodal input support, input validation, model registry querying, and descriptor metadata. The two crates work together as a classic interface/test-harness pair: core-llm defines the contract, and core-llm-testkit provides the tooling to verify that any downstream implementation correctly fulfills that contract. No cross-repository dependencies were identified, suggesting this repository is intended to be consumed as a library dependency by other projects that implement concrete LLM backends.

## Projects

### core-llm

core-llm is a Rust library that defines a trait-based abstraction layer for text-based Large Language Models (LLMs), along with a testkit (core-llm-testkit) that provides conformance testing utilities. The testkit allows implementors of the TextLlm trait to verify their implementations behave correctly across a suite of standardized behavioral checks covering streaming, cancellation, seeding, multimodal support, validation, registry, and descriptor functionality.

