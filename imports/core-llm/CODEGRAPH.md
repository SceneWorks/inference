# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 17:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library repository that provides a trait-based abstraction layer for text-based Large Language Model (LLM) backends. The repository consists of two cooperating crates: (1) the main `core-llm` crate, which defines the `TextLlm` trait and associated types that LLM backend implementors must satisfy — covering capabilities such as streaming output, cancellation, seed-based determinism, multimodal input support, input validation, and descriptor/registry metadata; and (2) `core-llm-testkit`, a companion testing crate that provides conformance test utilities so that any implementation of the `TextLlm` trait can be verified against the required behavioral contracts. The two crates work together as an interface-plus-testkit pattern: `core-llm` defines the contract, and `core-llm-testkit` provides reusable test harnesses to enforce it. No external cross-repository dependencies were identified in the analysis; the repository appears to be a foundational abstraction library intended to be consumed by downstream crates that implement concrete LLM backends (e.g., wrappers around OpenAI, Anthropic, local models, etc.).

## Projects

### core-llm

core-llm is a Rust library project that defines a trait-based abstraction for text-based Large Language Model (LLM) backends, along with a testkit (core-llm-testkit) that provides conformance testing utilities. The testkit allows implementors of the TextLlm trait to verify their implementations satisfy required behavioral contracts such as streaming, cancellation, seed determinism, multimodal support, validation, and descriptor/registry correctness.

