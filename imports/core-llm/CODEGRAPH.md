# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-21 23:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust repository that provides a trait-based abstraction layer for Large Language Model (LLM) text generation. It consists of two crates working in tandem: (1) the core library (`core-llm`) which defines the `TextLlm` trait and associated types representing the interface contract for LLM implementations (covering streaming output, multimodal input, seed-based determinism, and validation), and (2) a testkit crate (`core-llm-testkit`) that supplies conformance tests and stub/mock implementations. The testkit enables downstream implementors of the `TextLlm` trait to verify behavioral correctness against a standardized suite of checks covering streaming, cancellation, multimodal input, seed determinism, input validation, and registry/descriptor conformance. The repository appears to function as a foundational interface definition and quality-assurance toolkit — it does not implement any specific LLM backend itself, but instead establishes the contract that concrete LLM integrations must satisfy. No cross-repository dependencies were identified, meaning this library is intended to be consumed by external crates that implement LLM backends.

## Projects

### core-llm

core-llm is a Rust library/framework that defines a trait-based abstraction for Large Language Model (LLM) text generation, along with a testkit (core-llm-testkit) providing conformance tests and stubs. The testkit allows implementors of the TextLlm trait to verify their implementations against a standard set of behavioral checks including streaming, cancellation, multimodal input, seed determinism, validation, and registry/descriptor conformance.

