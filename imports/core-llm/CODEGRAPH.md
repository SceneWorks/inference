# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 00:08 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library repository that defines abstractions and conformance testing infrastructure for Large Language Model (LLM) integrations. The repository contains at least two related crates: the main core-llm crate, which likely provides a TextLlm trait and related types representing a common interface for LLM backends, and core-llm-testkit, a companion testing harness crate that supplies a suite of compliance checks verifiable against any TextLlm trait implementation. The testkit validates behavioral contracts across capabilities including streaming responses, cancellation, multimodal inputs, tool use, thinking/reasoning modes, seed-based determinism, and descriptor/registry metadata. Together, the two crates form a provider-agnostic LLM abstraction layer: core-llm defines the contract, and core-llm-testkit allows different backend implementations (e.g., wrappers around OpenAI, Anthropic, or local models) to prove conformance against that contract. No external cross-repository dependencies were identified, and no downstream consumers are known from the available evidence.

## Projects

### core-llm

core-llm is a Rust library project providing abstractions and conformance testing utilities for Large Language Model (LLM) integrations. The testkit subproject (core-llm-testkit) defines a suite of compliance checks and helper functions that verify any TextLlm trait implementation behaves correctly across capabilities such as streaming, cancellation, multimodal inputs, tool use, thinking/reasoning modes, seed determinism, and descriptor/registry metadata. It serves as a shared testing harness so that different LLM backend implementations can be validated against a common contract.

