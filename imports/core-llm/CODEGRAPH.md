# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-26 14:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library that defines a common abstraction layer for Large Language Model integrations. It provides a `TextLlm` trait (and associated types such as request/response structures) that serves as a vendor-neutral interface for text-based LLM interactions. The repository includes a testkit component that supplies a conformance test suite, allowing any implementor of the `TextLlm` trait to verify correct behavior across a range of capabilities including streaming responses, cancellation, multimodal (e.g., image) inputs, tool/function calling, and seed-based determinism. The two components work together in a typical Rust library pattern: the core crate defines the interface and data types, while the testkit crate consumes that interface to provide reusable, black-box conformance checks that third-party LLM adapter crates (e.g., wrappers for OpenAI, Anthropic, or local models) can run against their implementations. No external repository dependencies were identified, and nothing in the analysis indicates downstream consumers within this repository — it is designed to be depended upon by external LLM integration crates rather than depending on them.

## Projects

### core-llm

core-llm is a Rust library/framework providing abstractions and conformance testing infrastructure for Large Language Model (LLM) integrations. The project defines a TextLlm trait and related types, and the testkit component provides a suite of conformance checks and test utilities to verify that any implementation of the TextLlm trait correctly handles features such as streaming, cancellation, multimodal inputs, tool calling, seed determinism, and more.

