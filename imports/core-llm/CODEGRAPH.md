# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 17:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library that defines abstractions and a conformance test suite for Large Language Model (LLM) backends. It consists of at least two crates: the main `core-llm` crate, which provides core traits and types (such as `TextLlm`) that concrete LLM provider implementations must satisfy, and `core-llm-testkit`, a companion crate that supplies a reusable behavioral test harness. The testkit includes a stub/mock implementation and a standardized set of contract checks covering streaming output, cancellation, multimodal input, seed-based determinism, tool use, thinking mode, input validation, and provider registry/descriptor metadata. Together, the two crates form an abstraction layer plus a pluggable validation framework: library authors implement the `TextLlm` trait (or equivalent) against a specific LLM backend, then run the testkit suite to verify conformance. No external repository dependencies or downstream consumers were identified in the analysis.

## Projects

### core-llm

core-llm is a Rust library providing abstractions and a conformance test suite for Large Language Model (LLM) backends. The testkit subproject (core-llm-testkit) supplies a reusable set of behavioral checks and a stub implementation so that any concrete TextLlm provider can be validated against a standard contract covering streaming, cancellation, multimodal input, seed-based determinism, tool use, thinking mode, validation, and registry/descriptor metadata.

