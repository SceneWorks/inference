# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 16:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library repository that defines a trait-based abstraction layer for Large Language Model integrations. It consists of two closely related crates: (1) core-llm, which defines the core TextLlm trait and associated types that any LLM backend must implement, covering capabilities such as streaming text generation, cancellation, multimodal inputs (images and video), tool/function calling, thinking/reasoning modes, seed-based deterministic generation, model descriptors, and a model registry interface; and (2) core-llm-testkit, which provides a conformance testing harness that verifies any TextLlm implementation correctly satisfies the trait contract across all of those capability dimensions. The two crates work together as a provider-agnostic interface standard: library authors implement the TextLlm trait against a specific LLM backend (e.g., OpenAI, Anthropic, local models), and then use core-llm-testkit to validate correctness. No external repository dependencies were identified, but this library is likely intended to be consumed by downstream Rust crates that integrate specific LLM providers.

## Projects

### core-llm

core-llm is a Rust library providing abstractions and conformance testing utilities for Large Language Model (LLM) integrations. The project defines a TextLlm trait-based interface and provides a testkit (core-llm-testkit) with conformance checks that verify any TextLlm implementation correctly handles streaming, cancellation, multimodal inputs (images, video), tool use, thinking modes, seed-based determinism, descriptors, and registry behavior.

