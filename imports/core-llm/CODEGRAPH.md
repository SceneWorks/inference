# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-21 19:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust repository that provides a trait-based abstraction layer for text-based Large Language Model (LLM) backends. The main crate defines a common interface (likely a `TextLlm` trait) that different LLM backend implementations must satisfy, covering capabilities such as streaming responses, cancellation, seed-based determinism, multimodal input, input validation, a backend registry, and descriptor metadata. A companion crate, core-llm-testkit, provides a standardized conformance test harness that any `TextLlm` implementation can run against to verify correctness across all required behavioral contracts. The testkit also includes an internal stub implementation used to self-validate the harness itself. Together, the two crates form a foundation library: core-llm supplies the interface contract, and core-llm-testkit supplies the portable verification suite. No cross-repository dependencies were identified, suggesting this repository is intended to be consumed by downstream Rust projects that implement or integrate LLM backends, rather than depending on other internal repositories itself.

## Projects

### core-llm

core-llm is a Rust library that defines a trait-based abstraction for text-based Large Language Model (LLM) backends, along with a conformance test-kit (core-llm-testkit) that verifies any TextLlm implementation behaves correctly. The testkit provides a suite of standardized checks (streaming, cancellation, seed determinism, multimodal input, validation, registry, and descriptor correctness) plus a stub implementation used to self-test the conformance harness itself.

