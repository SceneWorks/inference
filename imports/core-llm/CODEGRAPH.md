# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-21 17:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust repository that defines a common abstraction layer for Large Language Model backends. The primary crate exposes a `TextLlm` trait and associated types (e.g., request/response structs, streaming primitives, error types) that any LLM backend implementation must satisfy. A companion crate, `core-llm-testkit`, provides a reusable conformance test suite and stub/mock implementations so that third-party or internal backend authors can verify their `TextLlm` implementations behave correctly across a defined set of behavioral contracts: streaming output, cancellation handling, seed-based determinism, multimodal input support, input validation, and registry/descriptor metadata contracts. The two crates work together as an interface-plus-verification package: `core-llm` supplies the stable API surface, while `core-llm-testkit` supplies the test harness that enforces correctness guarantees for anything implementing that API. No cross-repository dependencies were detected, suggesting this is a foundational library intended to be consumed by downstream crates or workspaces that provide concrete LLM backend implementations.

## Projects

### core-llm

core-llm is a Rust library providing abstractions and a conformance test suite for Large Language Model (LLM) backends. The project defines a `TextLlm` trait interface and associated types, while the `core-llm-testkit` crate provides reusable conformance checks and stub implementations to verify that any `TextLlm` implementation correctly handles streaming, cancellation, seed determinism, multimodal inputs, validation, and registry/descriptor contracts.

