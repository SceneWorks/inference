# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-21 18:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library repository that provides a trait-based abstraction layer for text-based large language models (LLMs). The repository consists of two crates: the core `core-llm` crate, which defines the primary `TextLlm` trait and associated types (covering streaming output, seeding, multimodal inputs, validation, and registry/descriptor interfaces), and `core-llm-testkit`, a companion crate that supplies conformance tests and stub/mock implementations. The testkit is intended for use by downstream implementors of the `TextLlm` trait so they can verify their implementations correctly satisfy the expected behavioral contract — including streaming behavior, cancellation handling, deterministic seeding, multimodal input support, input validation, and registry/descriptor requirements. The two crates work together as an interface definition plus verification harness: `core-llm` establishes the contract, and `core-llm-testkit` provides the tools to enforce it. No cross-repository dependencies were identified, suggesting this repository is a foundational library intended to be consumed by other projects that implement or wrap specific LLM backends.

## Projects

### core-llm

core-llm is a Rust library project that defines a trait-based abstraction for text-based large language models (LLMs), along with a testkit (core-llm-testkit) that provides conformance tests and stub implementations. The testkit allows implementors of the TextLlm trait to verify that their implementations correctly handle streaming, cancellation, seeding, multimodal inputs, validation, and registry/descriptor requirements.

