# core-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 00:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

core-llm is a Rust library that defines a common abstraction layer for Large Language Model (LLM) text generation backends. The repository contains two primary components: the core library (core-llm), which defines the TextLlm trait and associated types representing the contract that any LLM backend must fulfill, and a testkit (core-llm-testkit), which provides conformance test suites and stub implementations to verify that a given TextLlm implementation correctly handles streaming output, cancellation, multimodal inputs, seed-based deterministic generation, input validation, thinking/reasoning modes, and registry/descriptor behavior. Together, the two components form a backend-agnostic interface layer: the core crate defines what an LLM backend must do, and the testkit crate gives implementors a standard way to prove their backend satisfies that contract. No cross-repository dependencies were detected, suggesting this library is intended to be consumed by other crates or services that implement or depend on the TextLlm trait.

## Projects

### core-llm

core-llm is a Rust library providing abstractions and conformance testing infrastructure for Large Language Model (LLM) text generation backends. The project defines a TextLlm trait and associated types, and the testkit component (core-llm-testkit) provides a suite of conformance checks and stub implementations that verify whether a given TextLlm implementation correctly handles streaming, cancellation, multimodal inputs, seed-based determinism, validation, thinking modes, and registry/descriptor behavior.

