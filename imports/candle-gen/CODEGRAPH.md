# candle-gen

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-16 14:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based image generation framework built on top of the Candle ML library (HuggingFace's Rust/WASM ML framework). It implements diffusion model pipelines for text-to-image generation. The repository contains at least one subproject, candle-gen-chroma, which implements the Chroma image generation model in three variants (base, flash, and HD). Core technical components include: a noise scheduler using beta distribution-based sigma schedules for the diffusion process, model configuration structures for the different Chroma variants, and a generation pipeline that iteratively denoises latent representations conditioned on text prompts to produce images. The architecture follows the standard diffusion model pattern of encoding text prompts, iteratively applying a denoising model guided by a noise schedule, and decoding the result into an image. There are no observed cross-repository dependencies beyond the Candle ML library itself. Nothing in the provided evidence indicates external consumers of this library.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework built on the Candle ML library, implementing diffusion model pipelines. The candle-gen-chroma subproject specifically implements the Chroma image generation model in three variants (base, flash, HD), providing noise scheduling via beta distribution-based sigma schedules, model configuration, and a generation pipeline that converts text prompts to images through iterative denoising.

