# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-16 00:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based repository focused on generative image model inference, built on top of the Candle ML framework (HuggingFace's Rust/WASM ML framework). The primary subproject, candle-gen-chroma, implements a diffusion-based image generation pipeline targeting the Chroma model family (with base, HD, and flash variants). Key technical components include: noise scheduling via beta distributions, model configuration management, and a generation pipeline that handles noise initialization and sigma-based sampling schedules typical of diffusion models (e.g., similar to flow-matching or DDPM-style inference). The repository appears to be a standalone inference toolkit with no observed cross-repository dependencies, consuming Candle as its core tensor/ML backend. No external consumers have been identified. The overall architecture follows a pipeline pattern: configure model variant → initialize noise → apply iterative sigma-scheduled denoising steps → produce output images.

## Projects

### candle-gen

candle-gen is a generative image model inference project built on the Candle ML framework. The candle-gen-chroma subproject implements a diffusion-based image generation pipeline (likely for the Chroma model family with base, HD, and flash variants), including noise scheduling via beta distributions, model configuration, and a generation pipeline that handles noise initialization and sigma-based sampling schedules.

