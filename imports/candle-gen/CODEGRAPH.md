# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-25 00:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library for generative image model inference, built on top of the Candle ML framework (Hugging Face's Rust-native tensor library). It implements diffusion-based or flow-matching image generation pipelines, likely targeting Flux-style architectures. The repository exposes a Generator trait interface with at least two operational modes: a 'turbo' mode optimized for fast inference and an 'edit' mode that supports reference image conditioning (inpainting or image-to-image workflows). Core responsibilities include model weight loading, pipeline configuration, noise schedule computation (sigma and timestep calculations), and handling structured generation requests. As a single-project repository, all components serve the unified purpose of providing a backend library for image synthesis. There are no observed cross-repository dependencies, suggesting it is either self-contained or depends on published crates (such as candle-core, candle-nn, and potentially tokenizers or image processing crates) rather than local workspace siblings. The confidence is medium because the analysis is based on structural and naming evidence rather than full source inspection.

## Projects

### candle-gen

candle-gen is a Rust-based generative image model inference project that implements diffusion-based image generation pipelines (likely Flux or similar flow-matching models). It provides model loading, configuration, scheduling (sigma/timestep computation), and generation request handling for both a 'turbo' (fast) mode and an 'edit' mode that supports reference image conditioning. The project appears to be a backend library exposing a Generator trait interface for image synthesis.

