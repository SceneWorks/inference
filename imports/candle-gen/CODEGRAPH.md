# candle-gen

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-12 15:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust repository that implements an image generation inference pipeline built on top of HuggingFace's Candle ML framework. It is organized as two crates: (1) candle-gen, a core utility crate that defines a backend-agnostic generation interface (via a gen_core abstraction) and provides shared infrastructure for model loading, text encoding, latent diffusion scheduling, and VAE decoding; and (2) candle-gen-sdxl, a model-specific crate that implements and registers a Stable Diffusion XL (SDXL) backend conforming to that interface. The architecture separates the generic generation contract from the concrete SDXL implementation, suggesting the design anticipates additional model backends in the future. The primary function exposed is text-to-image (txt2img) generation. The repository depends on the Candle ML library (and likely its associated tokenizer and model-weight utilities) as its primary external dependency. No downstream consumers are identified in the provided analysis.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework that wraps the Candle ML library to provide a Stable Diffusion XL (SDXL) inference pipeline. It exposes a backend-agnostic generation interface (via gen_core) and implements model loading, text encoding, latent diffusion, and VAE decoding for txt2img generation. The project is structured as a core utility crate (candle-gen) plus a model-specific crate (candle-gen-sdxl) that registers the SDXL backend.

