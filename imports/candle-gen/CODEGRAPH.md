# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-15 15:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/framework for diffusion-model-based image generation, built on top of the Candle ML framework (a Rust-native tensor computation library). It targets the Chroma model family (base, HD, and flash variants) and provides the core infrastructure needed to run text-to-image inference: model loading and registration via a descriptor system, noise scheduling through beta-distribution-based sigma schedules, and configuration management. The repository appears to be a single-project workspace where all components (model descriptors, schedulers, configuration, and inference logic) reside together rather than being split across multiple crates. There are no observed cross-repository dependencies, suggesting it is either a standalone library or an early-stage project. Downstream consumers would likely be applications or services that call into candle-gen to generate images from text prompts using the supported Chroma model variants.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework that implements diffusion model inference, specifically targeting the Chroma model family (base, HD, and flash variants). It provides model loading, descriptor registration, noise scheduling via beta distribution-based sigma schedules, and configuration management for text-to-image generation using the Candle ML framework.

