# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-04 04:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust repository implementing a generative image inference pipeline, likely a diffusion model variant (evidenced by txt2img, image editing, reference image conditioning, and stream layer components). The repository is structured around the 'boogu' sub-crate, which handles model configuration, loading, building, and inference pipeline logic. It exposes a Generator trait supporting both base text-to-image generation and image editing workflows with reference image conditioning. The project appears to use the Candle ML framework (a Rust-native tensor/neural network library from Hugging Face) as its underlying compute backend. The overall architecture separates model definition and configuration from inference orchestration, with the Generator trait serving as the primary abstraction boundary for consumers. No cross-repository dependencies were identified, and it is unclear what external systems depend on this library. The business or application domain is not explicitly evident from the code structure alone; it is most accurately described as a Rust ML inference library for generative image models built on top of Candle.

## Projects

### candle-gen

candle-gen is a firmware/ML inference project that implements a generative image model pipeline (likely a diffusion model variant, given references to txt2img, edit components, reference conditioning, and stream layers). The 'boogu' sub-crate handles model configuration, loading, building, and inference pipeline logic, exposing a Generator trait that supports both base text-to-image generation and image editing workflows with reference image conditioning.

