# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 15:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/binary for image generation using diffusion models, built on top of the Candle ML framework (a Rust-native tensor computation library). The repository implements a diffusion model pipeline consistent with architectures like FLUX, supporting two primary use cases: text-to-image generation and instruction-based image editing (where a reference image and text instruction are combined to produce a modified output). Key technical components include model loading and configuration, noise scheduling and sigma computation (core to diffusion sampling), and a multi-stream transformer pipeline that processes text and image embeddings. The project appears to be self-contained with no observed cross-repository dependencies, relying externally on the Candle framework and standard Rust ML ecosystem crates. Its single project structure means all components (model loading, inference pipeline, noise scheduling) are co-located within one crate.

## Projects

### candle-gen

candle-gen is a Rust-based image generation project that implements a diffusion model pipeline (likely based on FLUX or a similar architecture) using the Candle ML framework. It provides functionality for text-to-image generation and image editing (instruction-based image editing with reference images), including model loading, configuration, noise scheduling/sigma computation, and a multi-stream transformer pipeline.

