# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 16:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based image generation backend (internally codenamed 'boogu') built on the Candle ML framework. It implements a diffusion-based generative model pipeline supporting two primary workflows: text-to-image generation and instruction-guided image editing (where a reference image and a text instruction are supplied as inputs). The repository is structured as a single Rust project handling the full inference pipeline, including model configuration management, noise scheduling (sigma and timestep computation), and tensor-level operations required for diffusion model inference. No cross-repository dependencies were identified, and no downstream consumers are known from the available evidence. The project appears to be a self-contained backend service or library for running diffusion model inference using Rust and Candle rather than Python-based frameworks like PyTorch or JAX.

## Projects

### candle-gen

candle-gen is a Rust-based image generation pipeline backend (codenamed 'boogu') that implements a diffusion-based generative model using the Candle ML framework. It supports text-to-image generation as well as image editing workflows (where a reference image and instruction are provided), managing model configuration, noise scheduling (sigma/timestep computation), and tensor-level operations for inference.

