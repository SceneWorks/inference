# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-02 13:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/backend implementing an image generation pipeline based on diffusion models (likely Flux or a similar architecture), referred to internally as a 'boogu' variant. It uses the Hugging Face `candle` ML framework for inference. The repository provides: model loading and configuration logic, a text-to-image and image-editing inference pipeline, and a plugin-style interface (`Box<dyn Generator>`) that integrates with an external `gen_core` framework responsible for orchestrating generation requests. The project appears to be a single-crate repository where all components work together as a self-contained generation backend — model configuration, pipeline execution, and the generator trait implementation are all part of one cohesive unit. It depends on `candle` (for tensor operations and model inference), `gen_core` (for the orchestration interface and request types), and likely standard Rust ML ecosystem crates. Nothing within this repository is known to depend on it externally, though it is likely consumed by a larger service or application via the `gen_core` plugin interface.

## Projects

### candle-gen

candle-gen is a Rust-based image generation backend implementing a diffusion model pipeline (likely Flux or similar), with support for text-to-image generation and image editing modes. The project provides model loading, configuration, and inference pipeline logic for a 'boogu' variant of a generator, exposing a plugin-style interface via `Box<dyn Generator>` and integrating with a `gen_core` framework for orchestrating generation requests.

