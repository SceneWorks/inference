# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-04 15:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust repository implementing a generative image model inference pipeline built on the Candle ML framework. The codebase centers on a single project (candle-gen-boogu) that handles the full inference stack for a diffusion-based image generation architecture—likely FLUX or a closely related model family. Key technical responsibilities include: loading and configuring model weights, computing inference schedules (sigma and shift values for the diffusion process), running the forward pass through the model, and supporting two distinct workflows—unconditional/text-conditioned image generation and reference-image-conditioned editing (inpainting or img2img style). The project appears to be self-contained with no observed cross-repository dependencies; it depends externally on the Candle ML framework (Hugging Face's Rust tensor library) and likely on standard Rust ecosystem crates for serialization and I/O. No other repositories are observed to depend on it. Overall the repository represents a standalone inference engine or experimentation harness for diffusion-based image generation in Rust.

## Projects

### candle-gen

candle-gen-boogu is a Rust project implementing a generative image model pipeline (likely a diffusion model such as FLUX or a similar architecture) using the Candle ML framework. It handles model configuration, weight loading, inference pipeline scheduling (sigma/shift computation), and supports both base generation and image editing workflows with reference image conditioning.

