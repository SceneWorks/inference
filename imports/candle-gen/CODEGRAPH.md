# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-25 18:13 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/backend for image generation using diffusion-based (likely flow-matching transformer, e.g., FLUX-style) generative models. It implements the core inference pipeline including model loading, configuration management, noise scheduling (shift schedules, sigmas), and sampling logic. The repository supports at least two inference modes: standard text-to-image ('turbo') and an image-editing ('edit') variant. It exposes a Generator trait as its primary abstraction for downstream consumers to drive inference. The project is built on the Hugging Face 'candle' Rust ML framework for tensor operations and model execution. As a single-project repository, all components—model architecture (single-stream transformer layers), scheduler, and generator interface—are co-located and work together as a unified inference backend. No cross-repository dependencies were detected, but the library is likely intended to be consumed by a higher-level application or service that handles user-facing concerns such as HTTP APIs or CLI interfaces.

## Projects

### candle-gen

candle-gen is a Rust-based image generation backend that implements a diffusion-based generative model pipeline (likely a FLUX or similar flow-matching transformer model, given references to 'single stream layers', shift schedules, and sigmas). It provides model loading, configuration, scheduling, and inference logic for both standard text-to-image ('turbo') and image-editing ('edit') variants, exposing a Generator trait for downstream consumers.

