# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-14 04:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library/framework for text-to-image generation built on top of the Candle ML library (Hugging Face's Rust-native ML framework). It implements a pipeline for running Flux diffusion models (Flux Schnell, Flux Dev, and a Flux2/Klein 9B variant). The architecture centers on a plugin/registry pattern: model descriptors are registered and resolved into generator instances that encapsulate model loading, configuration, sampling schedules, and the text-to-image inference loop. The repository appears to be a single-project crate (one Rust project) with no observed cross-repository dependencies consuming it, and it depends on the Candle ML ecosystem for tensor operations, model weights, and device management (CPU/GPU). Its primary technical responsibilities are: abstracting Flux diffusion model variants behind a common generator interface, managing model lifecycle (loading weights, configuring samplers), and executing the diffusion sampling pipeline to produce images from text prompts.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework that wraps the Candle ML library to provide a pipeline for running Flux diffusion models (including Schnell and Dev variants, as well as a Flux2/Klein 9B variant). It exposes a plugin/registry architecture where model descriptors are registered and resolved as generators, handling model loading, configuration, sampling schedules, and text-to-image inference surfaces.

