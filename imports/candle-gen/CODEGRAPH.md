# candle-gen

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-12 22:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library that implements a Stable Diffusion XL (SDXL) text-to-image generation pipeline using the Candle ML framework (Hugging Face's pure-Rust tensor/inference library). It encapsulates the full SDXL inference stack: CLIP text encoding, iterative diffusion sampling, and VAE decoding. The library conforms to a gen_core trait interface, registering itself as a 'candle' named provider, which indicates it is designed as one interchangeable backend within a larger, multi-backend image generation system. Other components in that system would select or dispatch to this backend by name at runtime. Optional compile-time features include flash attention (for memory-efficient attention computation) and VAE tiling (for generating large images with bounded VRAM). There are no observed cross-repository dependencies beyond the gen_core trait surface and the Candle ML crates themselves.

## Projects

### candle-gen

candle-gen is a Rust library that implements a Stable Diffusion XL (SDXL) image generation pipeline using the Candle ML framework. It exposes a standardized generation interface (conforming to a gen_core trait surface) that accepts text-to-image requests, runs CLIP text encoding, diffusion sampling, and VAE decoding to produce images. The project acts as a backend plugin/driver within a larger multi-backend image generation system, registering itself as a 'candle' provider and supporting optional features like flash attention and VAE tiling.

