# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-29 00:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library implementing an image generation pipeline backed by a diffusion/flow-matching model (likely Flux or a similar architecture). It uses the `candle` deep-learning framework for tensor operations and model inference. The repository provides: model loading and configuration, noise scheduling (sigma and shift calculations), a `Generator` trait implementation supporting both text-to-image and image-editing generation modes, and integration with a broader `gen_core` framework that defines shared abstractions. The single project within the repository acts as the concrete backend that translates high-level generation requests into model inference calls, managing the full pipeline from prompt encoding through denoising steps to image output. No external repositories depend on it based on available evidence, and it depends on `candle` (for ML primitives) and `gen_core` (for shared traits/types).

## Projects

### candle-gen

candle-gen is a Rust-based image generation pipeline project that implements a diffusion model backend (likely Flux or a similar flow-matching architecture), providing model loading, configuration, noise scheduling (sigma/shift calculations), and image editing capabilities. It exposes a Generator trait implementation that supports text-to-image and image-editing generation modes, integrating with a broader gen_core framework.

