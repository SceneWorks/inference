# candle-gen

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-12 18:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library that implements a Stable Diffusion XL (SDXL) text-to-image inference pipeline built on top of the Candle ML framework. It exposes a plugin/backend architecture where SDXL is registered as a named backend conforming to a gen_core interface. The pipeline covers the full SDXL inference stack: CLIP text encoding to produce conditioning embeddings, UNet-based iterative denoising over a latent space, and VAE decoding to reconstruct the final image. The repository bridges Candle's internal error types to the gen_core common error interface, suggesting it is designed to be consumed as a pluggable backend by a higher-level orchestration layer (likely a separate crate or binary that depends on gen_core). No downstream consumers or cross-repository dependencies were identified in the analysis, but the plugin registration pattern implies it is intended to be one of potentially several interchangeable generation backends.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework that wraps the Candle ML library to provide a Stable Diffusion XL (SDXL) inference pipeline. The project exposes a plugin/backend architecture where SDXL is registered as a named backend, handles text-to-image generation through a multi-step diffusion pipeline including CLIP text encoding, UNet denoising, and VAE decoding, and bridges Candle's error types to a common gen_core interface.

