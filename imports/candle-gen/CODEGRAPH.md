# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-17 23:30 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust library for text-to-image inference using diffusion models, built on top of the Candle ML framework. It implements a modular pipeline architecture centered on a Generator trait, with concrete implementations targeting the Chroma image generation model in multiple variants (base, flash, and HD). The library handles the key components of a diffusion inference pipeline: noise scheduling via beta distribution-based sigma schedules, model configuration and weight loading, and end-to-end pipeline execution. The repository appears to be a single-project library with no internal cross-project dependencies. It depends on the Candle ecosystem (HuggingFace's Rust ML framework) for tensor operations and model execution. There is no evidence of downstream consumers in this repository, suggesting it is either a standalone tool or an upstream dependency consumed by external projects. The overall architecture is inference-focused rather than training-focused, providing a clean abstraction layer over the underlying Candle primitives for diffusion-based image synthesis.

## Projects

### candle-gen

candle-gen is a Rust-based image generation framework that implements diffusion model pipelines, specifically targeting the Chroma image generation model in multiple variants (base, flash, HD). The project handles noise scheduling via beta distribution-based sigma schedules, model loading/configuration, and inference pipeline execution using the Candle ML framework. It appears to be a modular inference library providing a Generator trait implementation for text-to-image synthesis.

