# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-02 23:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based repository implementing a generative image model inference pipeline built on the Candle ML framework. It provides a diffusion-based image generation system (referred to as the 'boogu' variant) that supports two primary modes: text-to-image generation and image editing with reference image conditioning. The codebase is organized around model configuration and weight loading (with tensor normalization utilities), a diffusion sampling pipeline that executes the iterative denoising process, and a generator interface that validates inputs and resolves reference images for edit operations. As a single-project repository, all components work together as a unified inference stack: the generator interface accepts user requests, the configuration layer loads and normalizes model weights from disk, and the diffusion pipeline orchestrates the forward passes to produce output images. The project depends on the Candle ML framework (Hugging Face's Rust tensor/ML library) as its core computational backend, and likely depends on standard Rust crates for serialization and image I/O. No cross-repository dependencies or downstream consumers were identified from the available analysis.

## Projects

### candle-gen

candle-gen is a generative image model inference project built on the Candle ML framework (Rust). It implements a diffusion-based image generation pipeline ('boogu' variant) that supports text-to-image generation and image editing with reference conditioning. The project handles model configuration, weight loading with tensor normalization utilities, a diffusion sampling pipeline, and a generator interface that validates inputs and resolves reference images for edit operations.

