# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-01 01:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based image generation library built on top of the Hugging Face Candle ML framework. It implements a diffusion model pipeline (likely FLUX or a closely related architecture) that supports text-to-image generation and instruction-based image editing. The repository is organized as a Cargo workspace with at least two crates: a core pipeline crate ('candle-gen') and a model-specific sub-crate ('boogu'). The core crate handles sigma-based noise scheduling, model configuration management, and a generator interface that integrates with an external 'gen_core' framework. The 'boogu' sub-crate implements a specific model variant featuring reference image conditioning and multi-stream transformer layers, suggesting support for style or content-guided generation. The repository appears to be a backend inference component — it consumes ML model weights and provides generation APIs — rather than a training framework or end-user application. No downstream dependents or cross-repository consumers were identified in the analysis. Overall confidence is medium because the exact business domain is not evident from the code structure alone, and some architectural details (e.g., the full interface contract with 'gen_core') remain inferred rather than directly observed.

## Projects

### candle-gen

candle-gen is a Rust-based image generation pipeline implementation that provides text-to-image and image-editing (instruction-based) generation capabilities using a diffusion model architecture (likely FLUX or similar). It implements a scheduler/pipeline with sigma-based noise scheduling, model configuration management, and a generator interface that integrates with a broader gen_core framework. The 'boogu' sub-crate appears to implement a specific model variant with support for reference image conditioning and multi-stream transformer layers.

