# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-27 22:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based firmware/ML inference repository focused on generative image synthesis. It implements a diffusion-based image generation pipeline (likely Flux or a similar architecture), providing: model loading and configuration, noise scheduling via sigma computation, layer normalization, multi-stream transformer layer configuration, and a Generator trait that supports both text-to-image and image-editing generation modes. The repository appears to be a single-project codebase built on top of the Hugging Face 'candle' ML framework (a lightweight Rust tensor library), wrapping it with higher-level pipeline abstractions. There are no identified cross-repository dependencies, suggesting it operates as a self-contained library or binary. Its likely consumers would be applications or services that need on-device or server-side generative image inference without a Python runtime.

## Projects

### candle-gen

candle-gen is a firmware/ML inference project implementing a generative image model pipeline (likely a diffusion-based model such as Flux or similar), providing model loading, configuration, scheduling, and generation logic. It exposes a Generator trait implementation that supports text-to-image and image-editing generation modes, with pipeline components including noise scheduling (sigmas), layer normalization, and multi-stream transformer layer configuration.

