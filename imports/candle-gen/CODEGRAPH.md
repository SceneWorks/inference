# candle-gen

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-29 02:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-gen is a Rust-based ML inference repository focused on diffusion-based image generation pipelines. It uses the Candle ML framework (a Rust tensor library) to implement text-to-image and image-editing workflows. The core library provides a Generator abstraction that manages timestep scheduling, sigma noise schedules, and model configuration. A subproject called 'boogu' implements a specific model variant with transformer-based architecture (likely Flux or a similar diffusion transformer), supporting reference-image conditioning and multi-stream transformer layers. The projects appear to be tightly coupled: the main candle-gen crate provides shared infrastructure (scheduling, pipeline orchestration, configuration), while boogu implements a concrete model backend. There are no observed cross-repository dependencies; the primary external dependency is the Candle ML framework. Nothing is observed to depend on this repository externally. The overall purpose is on-device or server-side ML inference for image generation rather than training.

## Projects

### candle-gen

candle-gen is a firmware/ML inference project implementing a diffusion-based image generation pipeline (likely a Flux or similar transformer-based model) using the Candle ML framework in Rust. It provides a Generator implementation that supports text-to-image and image-editing workflows, including timestep scheduling, sigma noise schedules, and model configuration management. The 'boogu' subproject appears to be a specific model variant with support for reference-image conditioning and multi-stream transformer layers.

