# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 15:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library/application for running large language model (LLM) inference, built on the Candle ML framework (HuggingFace's Rust-native tensor computation library). The repository's core responsibility is supporting multiple LLM architectures — including LLaMA, Gemma, Qwen, DeepSeek, GLM, and others — by parsing their JSON configuration files, dispatching to architecture-specific model implementations, and managing model hyperparameters such as attention head counts, rotary position embeddings (RoPE), key-value (KV) cache settings, and mixture-of-experts (MoE) configurations. The config.rs module acts as the central configuration hub, deserializing model configs and exposing computed, architecture-specific properties. The project is self-contained within a single Rust crate; no cross-repository dependencies were identified. It depends on the Candle ML framework and standard Rust ecosystem crates (serde for JSON deserialization, etc.). It is likely consumed as either a standalone inference binary or a library by downstream applications that need multi-architecture LLM inference in Rust.

## Projects

### candle-llm

candle-llm is a Rust project that implements large language model (LLM) inference, built on top of the Candle ML framework. The project handles configuration parsing and dispatch for multiple LLM architectures (LLaMA, Gemma, Qwen, DeepSeek, GLM, etc.), managing model hyperparameters such as attention heads, rotary position embeddings (RoPE), key-value cache configurations, and mixture-of-experts settings. Its config.rs module serves as the central hub for deserializing model configs from JSON and providing architecture-specific computed properties.

