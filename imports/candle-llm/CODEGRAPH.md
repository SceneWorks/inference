# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 11:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library/application for running large language model (LLM) inference on multiple model architectures. It is built on top of HuggingFace's Candle ML framework (a Rust-native tensor computation library). The repository provides a unified configuration parsing and dispatch layer that deserializes JSON model configs and routes to architecture-specific implementations covering Llama, Gemma, Qwen, DeepSeek, GLM, and other transformer-based LLMs. Key technical responsibilities include: (1) parsing and normalizing model hyperparameters such as hidden dimensions, attention head counts, RoPE (Rotary Position Embedding) settings, MoE (Mixture of Experts) routing parameters, and MLA (Multi-head Latent Attention) configurations; (2) dispatching to the correct model architecture implementation based on the parsed config; (3) managing inference execution within Candle's computation graph. The project appears to be self-contained with no detected cross-repository dependencies beyond its reliance on the Candle ML framework and standard Rust ecosystem crates (likely serde for JSON deserialization). It is most likely used as an end-user inference tool or as a library that other Rust applications could depend on to embed LLM inference capabilities.

## Projects

### candle-llm

candle-llm is a Rust project for running large language model (LLM) inference, built on top of the Candle ML framework. The project handles configuration parsing and dispatch for multiple LLM architectures (Llama, Gemma, Qwen, DeepSeek, GLM, etc.), managing model hyperparameters such as attention heads, RoPE settings, MoE (Mixture of Experts), and MLA (Multi-head Latent Attention). Its config.rs module serves as the central hub for deserializing model JSON configs and routing to architecture-specific logic.

