# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 16:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library for LLM inference built on top of HuggingFace's Candle ML framework. It provides: (1) configuration parsing and model architecture support for multiple LLM families including LLaMA, Gemma2, Qwen, DeepSeek, GLM4, and others; (2) attention mechanism implementations including Rotary Position Embeddings (RoPE), Multi-Head Latent Attention (MLA), Grouped-Query Attention (GQA), and Mixture-of-Experts (MoE) routing; (3) batch decoding infrastructure for token streaming and autoregressive generation. The repository is a single-project library (no internal sub-projects) that depends on the Candle ML framework for tensor operations and model execution. It abstracts over architectural differences between LLM variants, allowing a unified inference pipeline to serve multiple model families. No known downstream consumers are identified from the analysis, but it is likely used as a dependency in applications requiring local LLM inference in Rust.

## Projects

### candle-llm

candle-llm is a Rust-based LLM (Large Language Model) inference engine built on top of the Candle ML framework. It handles configuration parsing for various LLM architectures (LLaMA, Gemma2, Qwen, DeepSeek, GLM4, etc.), manages attention mechanisms including RoPE (Rotary Position Embeddings), multi-head latent attention (MLA), mixture-of-experts (MoE), and grouped-query attention (GQA), and provides batch decoding infrastructure for token streaming.

