# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-28 22:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library built on top of the Hugging Face Candle ML framework that provides infrastructure for loading, configuring, and running large language model (LLM) inference. The repository parses model configuration files (JSON-format configs from model directories) to extract hyperparameters such as attention scaling factors, RoPE (Rotary Position Embedding) settings, KV-cache head counts, MLA (Multi-head Latent Attention), MoE (Mixture of Experts) parameters, and quantization settings. Based on these configurations, it dispatches to specific architecture implementations including LLaMA, Gemma2, Qwen, DeepSeek, and GLM4. The project appears to be a single cohesive Rust crate (no multi-project structure is evident) focused on the model-loading and architecture-dispatch layer of LLM inference, abstracting over multiple model families through a unified configuration parsing and dispatch mechanism. It has no identified cross-repository dependencies beyond its likely dependency on the Candle tensor computation framework itself.

## Projects

### candle-llm

candle-llm is a Rust project that provides infrastructure for loading, configuring, and dispatching large language model (LLM) architectures. It parses model configuration files (likely JSON-format configs from model directories), extracts model hyperparameters such as attention scaling, RoPE (Rotary Position Embedding) settings, KV-head counts, MLA (Multi-head Latent Attention), MoE (Mixture of Experts), and quantization settings, and dispatches to specific architecture implementations (e.g., LLaMA, Gemma2, Qwen, DeepSeek, GLM4).

