# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 16:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library that provides configuration parsing and model architecture dispatching for large language models (LLMs). It reads and normalizes JSON/YAML model configuration files (analogous to HuggingFace config.json files) for multiple LLM architectures including Llama, Gemma2, Qwen2/Qwen3, DeepSeek, and GLM4. The library extracts and normalizes architecture-specific parameters such as attention head counts, RoPE (Rotary Position Embedding) settings, quantization options, and Mixture-of-Experts (MoE) configurations. Based on these parsed configurations, it dispatches to the appropriate model architecture implementation, likely using the Candle ML framework (Hugging Face's Rust-based tensor computation library) as its inference backend. The repository appears to be a single-project codebase focused on the configuration and model-selection layer of LLM inference, serving as the glue between serialized model metadata and the underlying compute primitives. No cross-repository dependencies were identified, suggesting it is either self-contained or its dependencies are declared via Cargo.toml without being separately analyzed.

## Projects

### candle-llm

candle-llm is a Rust project that provides configuration parsing and model architecture dispatching for large language models (LLMs). It handles reading and normalizing JSON/YAML model configuration files (similar to HuggingFace config.json files) for various LLM architectures including Llama, Gemma2, Qwen2/Qwen3, DeepSeek, and GLM4, extracting parameters like attention heads, RoPE settings, quantization, and MoE configurations needed to instantiate inference backends (likely using the Candle ML framework).

