# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-21 20:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library for LLM inference built on the Candle ML framework (Hugging Face's Rust-native tensor library). It implements the full inference pipeline for multiple large language model architectures, with confirmed support for Llama and Qwen3 families. Key technical components include: configuration parsing for different model architectures (reading model hyperparameters such as hidden size, number of attention heads, vocabulary size, etc.), a token generation engine with streaming output support, device selection logic (CPU/GPU/Metal), transformer model implementations featuring multi-head attention with KV caching for efficient autoregressive generation, and RoPE (Rotary Position Embedding) for positional encoding. The repository is structured as a single Rust library crate. It depends on the Candle ecosystem (candle-core, candle-nn, candle-transformers) for tensor operations and model primitives, and likely on tokenizers for text encoding/decoding. There are no observed reverse dependencies—this appears to be a standalone inference library intended to be used as a dependency by downstream applications that need to run LLM inference in Rust without Python overhead.

## Projects

### candle-llm

candle-llm is a Rust library for running large language model (LLM) inference, built on top of the Candle ML framework. It provides configuration parsing for multiple LLM architectures (Llama, Qwen3, etc.), token generation with streaming support, device selection, and model implementations including attention mechanisms with KV caching and RoPE embeddings. The project appears to be a general-purpose LLM inference engine that supports multiple model families.

