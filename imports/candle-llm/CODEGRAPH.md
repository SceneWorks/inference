# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-21 23:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library for LLM inference built on the Candle ML framework. It provides a unified pipeline for loading and running large language models, covering: (1) model configuration parsing for multiple architectures (LLaMA, Qwen3, and likely others); (2) GGUF file format loading for quantized model weights; (3) tokenization via the tokenizers ecosystem; (4) device abstraction supporting CPU and GPU backends; and (5) autoregressive text generation with streaming output and optional constraint-based (structured) decoding. The repository appears to be a single Rust crate with no internal sub-projects — all components work together as one cohesive inference library. It depends on the Candle ML ecosystem (candle-core, candle-transformers, etc.) and the HuggingFace tokenizers library. No downstream dependents are evidenced within the repository itself, suggesting it is intended as a reusable library or binary tool for end-users or application developers who want to run quantized LLMs locally in Rust.

## Projects

### candle-llm

candle-llm is a Rust library for running large language model (LLM) inference, built on top of the Candle ML framework. It handles model configuration parsing (supporting multiple architectures like LLaMA, Qwen3), GGUF file format loading, tokenization, device selection (CPU/GPU), and autoregressive text generation with streaming output and optional constraint-based decoding.

