# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 15:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library/application for running large language model (LLM) inference, built on top of the Candle ML framework (a Rust-native tensor computation library). The repository provides a unified abstraction layer for loading, configuring, and executing inference across multiple LLM architectures including Llama, Gemma, Qwen, DeepSeek, GLM, and others. A central config.rs module normalizes the diverse JSON configuration formats from different model families into a single internal representation, handling architecture-specific hyperparameters such as attention head counts, key-value cache dimensions, rotary position embeddings (RoPE), mixture-of-experts (MoE) routing, and multi-head latent attention (MLA). The project is a single cohesive unit rather than a multi-project monorepo — the configuration layer feeds directly into model construction and inference execution. It depends on the Candle ML framework for tensor operations and likely on Hugging Face model weight formats for loading pretrained models. There are no identified reverse dependencies (nothing is known to depend on this repository externally).

## Projects

### candle-llm

candle-llm is a Rust project for running large language model (LLM) inference, built on top of the Candle ML framework. The project handles configuration parsing and dispatch for multiple LLM architectures (Llama, Gemma, Qwen, DeepSeek, GLM, etc.), managing model hyperparameters such as attention heads, KV cache dimensions, rotary position embeddings (RoPE), mixture-of-experts (MoE), and multi-latent attention (MLA). The config.rs file serves as the central configuration layer that normalizes diverse model JSON configs into a unified internal representation used to build and run inference.

