# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 13:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library/binary for LLM inference built on top of the Candle ML framework (HuggingFace's Rust tensor library). It implements multiple LLM architectures from scratch in Rust, including LLaMA, Gemma2, Qwen2/3, GLM4, DeepSeek V2/V3, Mixtral (Mixture-of-Experts), and others. Core technical capabilities include: loading pretrained model weights (likely from HuggingFace-compatible formats), configuring architecture-specific parameters (attention heads, hidden dimensions, RoPE settings, MoE routing), and running autoregressive text generation with batched and streamed token decoding. Advanced attention mechanisms are supported, including grouped-query attention (GQA), multi-head latent attention (MLA as used in DeepSeek), and rotary position embeddings (RoPE) with various scaling strategies. The repository appears to be a single-project codebase with no cross-repository dependencies identified. It depends on the Candle framework for tensor operations and likely on HuggingFace tokenizer libraries. There are no identified downstream dependents, suggesting it functions as a standalone inference engine or developer tool for running open-weight LLMs locally using Rust.

## Projects

### candle-llm

candle-llm is a Rust-based LLM (Large Language Model) inference engine built on the Candle ML framework. It handles loading, configuring, and running inference for multiple LLM architectures (LLaMA, Gemma2, Qwen2/3, GLM4, DeepSeek V2/V3, Mixtral MoE, etc.) with support for features like grouped-query attention (GQA), multi-head latent attention (MLA), rotary position embeddings (RoPE), mixture-of-experts (MoE), and batched/streamed token decoding.

