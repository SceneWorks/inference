# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-25 15:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust-based LLM (Large Language Model) inference engine built on top of the Candle deep learning framework (HuggingFace's Rust ML framework). It implements a batched token decoding pipeline supporting multiple LLM architectures including Llama, Gemma2, Qwen2-MoE, DeepSeek, GLM4, and others. The codebase handles: (1) architecture-specific configuration parsing for each supported model family, (2) attention mechanisms including Rotary Position Embedding (RoPE), Multi-Head Latent Attention (MLA), Grouped-Query Attention (GQA), and Mixture-of-Experts (MoE) routing, (3) a batched inference scheduler for managing concurrent token generation requests. As a single-project repository, all components work together as one inference runtime. It depends on the Candle framework for tensor operations and model execution, and likely on HuggingFace model weights/tokenizers for model loading. No downstream dependents were identified in the analysis.

## Projects

### candle-llm

candle-llm is a Rust-based LLM inference engine built on the Candle framework. It handles configuration parsing for multiple LLM architectures (Llama, Gemma2, Qwen2-MoE, DeepSeek, GLM4, etc.), manages attention mechanisms including RoPE (Rotary Position Embedding), multi-head latent attention (MLA), grouped-query attention, and mixture-of-experts (MoE), and implements a batched token decoding pipeline with scheduling.

