# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 00:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library/application for running large language model (LLM) inference using the Candle ML framework (a Rust-native tensor computation library developed by Hugging Face). The repository centers on parsing and dispatching model configurations for multiple LLM architectures including LLaMA, Gemma, Qwen, DeepSeek, GLM, and others. Key technical responsibilities include: (1) reading and deserializing model configuration files (JSON/directory-based, as used by Hugging Face model repositories), (2) supporting architectural variants such as grouped-query attention, rotary position embeddings (RoPE) with various scaling strategies, quantization modes, and mixture-of-experts (MoE) layers, (3) dispatching to architecture-specific inference implementations based on parsed configuration. The project appears to be a single Rust crate without identified cross-repository dependencies beyond Candle itself. It is likely intended as either a standalone CLI tool or a library enabling downstream Rust applications to run LLM inference locally, analogous to projects like llama.cpp but in the Rust/Candle ecosystem. No external dependents were identified in this analysis.

## Projects

### candle-llm

candle-llm is a Rust project for running large language model (LLM) inference, built on top of the Candle ML framework. It handles configuration parsing for multiple LLM architectures (LLaMA, Gemma, Qwen, DeepSeek, GLM, etc.), including attention mechanisms, rotary position embeddings (RoPE), quantization, and mixture-of-experts (MoE) variants. The config.rs file shown here is central to parsing and dispatching model configurations from JSON/directory-based model definitions.

