# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 13:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library/application for running large language model (LLM) inference on top of the Hugging Face Candle deep learning framework. The repository centers on a configuration and dispatch layer (config.rs) that deserializes model hyperparameters from JSON or directory-based layouts and routes execution to architecture-specific implementations. Supported model families include Llama, Gemma, Qwen, DeepSeek, GLM, and other transformer-based LLMs. The code manages architectural parameters such as attention head counts, rotary position embeddings (RoPE), grouped-query attention (GQA) key-value cache configurations, and mixture-of-experts (MoE) variants. As a single-project repository, all components work together within one Rust crate: config parsing feeds derived parameters into model constructors, which perform forward-pass inference using Candle's tensor primitives. The primary external dependency is the Hugging Face Candle framework (and likely its associated tokenizers/hub crates). No downstream dependents were identified from the available evidence. The project appears to be a standalone inference runtime rather than a reusable library published to crates.io, though it could serve either purpose.

## Projects

### candle-llm

candle-llm is a Rust project for running large language model (LLM) inference, built on top of the Hugging Face Candle framework. The project handles configuration parsing and dispatch for multiple LLM architectures (Llama, Gemma, Qwen, DeepSeek, GLM, etc.), managing model hyperparameters such as attention heads, rotary position embeddings (RoPE), key-value cache grouping, and mixture-of-experts (MoE) variants. The config.rs file serves as the central hub for deserializing model configs from JSON/directory layouts and computing derived architectural parameters.

