# candle-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 00:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

candle-llm is a Rust library implementing LLM inference on top of the Candle ML framework. It is a single-project repository with no detected cross-repo dependencies. The library handles the full inference pipeline: reading model configuration files from disk (supporting architectures including LLaMA, Gemma2, Qwen2, Qwen3, and Mixture-of-Experts variants), constructing model-specific attention mechanisms and RoPE positional encoding parameters, managing a KV-cache scheduler for batched token generation, and running autoregressive decoding with configurable sampling strategies. It exposes cancellation support and event streaming for incremental (streaming) output. The project appears designed to be embedded as a dependency in applications that need on-device or self-hosted LLM inference in Rust, rather than being an end-user application itself.

## Projects

### candle-llm

candle-llm is a Rust library for running large language model (LLM) inference, built on top of the Candle ML framework. It handles model configuration parsing (supporting multiple architectures like LLaMA, Gemma2, Qwen2/3, and MoE variants), batched token generation with KV-cache scheduling, and streaming decode with sampling. The project serves as a self-contained inference engine that reads model configs from disk, builds appropriate attention/RoPE parameters, and runs autoregressive generation with cancellation and event streaming support.

